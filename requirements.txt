discord.py==2.3.2
aiohttp==3.9.1
python-dotenv==1.0.0
Flask==3.0.0

cd C:\Users\burry\PycharmProjects\DiscordBot

# Add the requirements file
git add requirements.txt

# Commit it
git commit -m "Add requirements.txt for Render deployment"

# Push to GitHub
git push origin main
```

echo "# Discord-Bot" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Mmounce1/Discord-Bot.git
git push -u origin main
